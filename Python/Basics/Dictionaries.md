{Key: Value}
```python
my_dictionary = {
	"entry1": "First person",
	"entry2": "Last person"
}

```

#### Create new entry:
`my_dictionary["entry3"] = "Another person"`

#### Empty the Directory
`empty_dictionary = {}`


>[!TIP] About Iteration
>When you run a for loop in dictionaries, it will only give the key values


#### To list the key-value of dict in a loop:
```python
for question in question_data:
	print(question['text'])
```
# Functions
![[Built-in Functions#Dictionary Manipulation]]